{
  "name": "@intus/selectme",
  "version": "1.0.0",
  "description": "Provides a searchable multi or single select",
  "main": "SelectMe.vue",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vue build --target lib --name SelectMe --dest dist/SelectMe SelectMe.vue"
  },
  "keywords": [
    "select",
    "searchable",
    "dropdown",
    "responsive"
  ],
  "author": "Pedro Del Moral Lopez",
  "license": "ISC",
  "devDependencies": {
    "@vue/cli": "^4.1.2"
  }
}